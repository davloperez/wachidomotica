# @esphome-device
# Persiana Dormitorio 2 Configuration
# Imports shared logic from persiana-base.yaml

substitutions:
  device_id: "3"
  open_duration: "29" # Tiempo para subir (segundos)
  close_duration: "28" # Tiempo para bajar (segundos)

esphome:
  name: persiana-dormitorio-2

packages:
  base: !include persiana-base.yaml
  dormitorio_base: !include persiana-dormitorio-base.yaml

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password
  power_save_mode: none
  output_power: 20.0dB

  # Enable fallback hotspot (captive portal) in case wifi connection fails
  ap:
    password: "platano+1234"
    ssid: "AP-Persiana-Dormitorio-2"
